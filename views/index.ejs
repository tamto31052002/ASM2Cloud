<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Toy Store</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f8f9fa;
        }
        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1, h2 {
            color: #333;
            margin-bottom: 20px;
        }
        form {
            margin-bottom: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            padding: 10px;
            border: 1px solid #ccc;
        }
        th {
            background-color: #007bff;
            color: #fff;
            text-align: left;
        }
        td {
            text-align: center;
        }
        button {
            padding: 8px 16px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>ATN company Website.</h1>
        <form action="/quotes" method="POST">
            <input type="text" placeholder="Name of Product" name="name" />
            <input type="text" placeholder="Price" name="price" /> 
            <button type="Add product">Add Product</button>
        </form>

        <h2>Table of Product </h2>
        <table>
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Price</th> 
                    <th>Action</th> 
                </tr>
            </thead>
            <tbody>
                <% for(var i = 0; i < quotes.length; i++) { %>
                    <tr>
                        <td><%= quotes[i].name %></td>
                        <td><%= quotes[i].price %></td> 
                        <td><button class="delete-button" data-index="<%= i %>">Delete</button></td> 
                    </tr>
                <% } %>
            </tbody>
        </table>
        </table>
        
        
    <script src="/main.js"></script>
    <script>
        document.querySelectorAll('.delete-button').forEach(function(button) {
            button.addEventListener('click', function() {
                var index = this.getAttribute('data-index');
                this.parentNode.parentNode.remove();
            });
        });
    </script>
</body>
</html>
